V8.0.2 SYNTAX HOTFIX

Fixes Python f-string syntax errors in scripts/generate_profile.py caused by escaped XML quotes inside f-string expressions.

Changed implementation:
- animation XML is built in local variables before interpolation;
- all known instances of the same invalid pattern were corrected;
- no visual design, profile text, workflow logic, or dependency versions were changed.

Validated:
- python -m py_compile scripts/generate_profile.py
- python -m py_compile scripts/cleanup_repo.py
- animated demo generation
- static demo generation
