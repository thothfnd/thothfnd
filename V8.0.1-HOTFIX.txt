THOTHFND PROFILE V8.0.1 — DEPENDENCY HOTFIX

Cause:
  numpy==2.3.2 conflicted with opencv-python-headless==4.12.0.88.

Fix:
  numpy is pinned to 2.2.6, which satisfies OpenCV's Python 3.11 requirement:
  numpy >= 2 and < 2.3.0.

No V8 design, renderer, workflow behavior, cleanup logic, or profile data was changed.

INSTALL:
  Replace only requirements.txt in the repository, then rerun:
  Actions -> Refresh immersive profile -> Run workflow
