# THOTH /FND V3 — System Build

`README.md` is generated by `scripts/build_profile.py` and is intended to be the GitHub profile README.

Use `README-SOURCE.md` for implementation notes so the generator never destroys documentation.
